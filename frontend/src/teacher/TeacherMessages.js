import React from 'react';

const TeacherMessages = ({ user }) => {
  return (
    <div className="admin-card">
      <h3>Messages & Announcements</h3>
      <p>Send messages to your students or read incoming questions.</p>
    </div>
  );
};

export default TeacherMessages;